import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import Heading from '../../components/settings/heading'

const language = () => {
  return (
    <View>
       <Heading title={'Select language'} />
    </View>
  )
}

export default language

const styles = StyleSheet.create({})